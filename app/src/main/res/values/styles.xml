<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/material_orangeA400</item>
        <item name="colorPrimaryDark">@color/material_orangeA400</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:navigationBarColor">@color/material_orangeA400</item>
    </style>
    <style name="Splash" parent="AppTheme">
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@drawable/splash_view</item>
    </style>

</resources>
